@{
  Layout = "_Layout";
}

@using Podcast.Models;
@model List<Podcast.Models.Title>;

<h2>Podcasts:</h2>
<hr>

@if (@Model.Count == 0)
{
  <h3>No podcasts have been added yet!</h3>
} 

@foreach (Title title in Model)
{
  <div class="card-deck">
    <div class="card text-center bg-light border-dark mb-3" style="width: 24rem;">
      <div class ="card-body">
        <h5 class="card-title">@Html.ActionLink($"{title.PodcastName}", "Details", new { id = title.TitleId }, new { @class = "text" })</h5>
        <p class ="card-text">@title.Description</p>
      </div>
    </div>
  </div>
}

<br>
<p class ="btn btn-light">@Html.ActionLink("Search for a Podcast", "Search")</p>
<p class="btn btn-light">@Html.ActionLink("Add New Podcast", "Create")</p>
<p class="btn btn-light">@Html.ActionLink("Add A Category", "AddCategory")</p>
<p class="btn btn-light">@Html.ActionLink("Return Home", "Index", "Home")</p>
